apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: polarity-server-integrations
  namespace: {{ .Values.namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi